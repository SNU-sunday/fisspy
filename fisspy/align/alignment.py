from .base import AlignOffset, rotImage, shiftImage
import numpy as np
import matplotlib.pyplot as plt


def calAlignPar(lcamA, lcamB, refFrame=None, save=False):
    if len(lcamB) and nf:
        nf = len(lcamA)
        raise ValueError("Inputs fHas and FCas in FissAlign should have the same number of elements!")
    
def align2cams(imA, imB):
    None

def alignRot(flist):
    None

def makeAlignCube(save=False):
    None

class Align:
    """
    """
    def __init__(self, lcamA, lcamB, refFrame=None):
        None
        
        